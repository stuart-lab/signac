% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alphagenome.R
\name{LoadAVIFromAtlas}
\alias{LoadAVIFromAtlas}
\title{Query AVI scores from the AlphaGenome Atlas API}
\usage{
LoadAVIFromAtlas(
  region,
  api.key = NULL,
  scorer = "AVI_SCORE_FEATURE_IMPORTANCE",
  max.workers = 4,
  max.retries = 5,
  cache = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{region}{Genomic region (\link[GenomicRanges:GRanges]{GenomicRanges::GRanges} or a string that can
be converted to \code{GRanges}, such as "chr11:5225000-5230000").}

\item{api.key}{AlphaGenome API key. If \code{NULL}, the
\code{ALPHA_GENOME_API_KEY} environment variable is used.}

\item{scorer}{Name of the Atlas variant scorer to retrieve. The default,
\code{"AVI_SCORE_FEATURE_IMPORTANCE"}, is the AVI SHAP feature attributions.
Other useful scorers are \code{"AVI_SCORE"} (the summary AVI score) and
\code{"AVI_SCORE_MODEL_FEATURES"} (the 18 raw AVI input features). Use
\code{\link[=ListAtlasScorers]{ListAtlasScorers()}} to see all available scorer names.}

\item{max.workers}{Number of requests sent to the Atlas API concurrently.
Lower this if the API responds with "The service is currently unavailable",
which can indicate rate limiting.}

\item{max.retries}{Maximum number of times a throttled request is retried
before the query fails (see Details).}

\item{cache}{Reuse results fetched earlier in the session and only query
the API for parts of the region that have not been fetched yet.}

\item{verbose}{Display progress messages}
}
\value{
Returns a \code{data.frame} with one row per variant, containing
the columns \code{chromosome}, \code{position}, \code{ref}, \code{alt},
and one numeric column per score returned by the scorer (for AVI SHAP,
one column per feature). The data frame can be passed directly to
\code{\link[=AVITrack]{AVITrack()}} or the \code{avi} argument of \code{\link[=CoveragePlot]{CoveragePlot()}}.
}
\description{
Retrieve AlphaGenome Variant Impact (AVI) scores or AVI SHAP feature
attributions for all SNVs in a genomic region directly from the AlphaGenome
Atlas API, without downloading the Atlas files.
}
\details{
The Atlas API is a gRPC service that is accessed through the \code{alphagenome}
Python package. This function uses reticulate to call the Python client, so
both reticulate and the \code{alphagenome} Python package must be installed.

An AlphaGenome API key is required (see
\url{https://deepmind.google.com/science/alphagenome}). The key can be
passed using the \code{api.key} argument or stored in the
\code{ALPHA_GENOME_API_KEY} environment variable (for example in your
\verb{~/.Renviron} file or by running \code{Sys.setenv(ALPHA_GENOME_API_KEY = ...)}.

Each position in the region carries three SNVs. The Atlas API serves these
in 32 bp chunks (about 0.5 s per request), so querying large regions can
take some time.

Results are cached in memory for the duration of the R session
(\code{cache = TRUE}), so plotting the same or an overlapping region again
only queries the API for the parts of the region that have not been
fetched before. Use \code{\link[=ClearAtlasCache]{ClearAtlasCache()}} to discard the cached results.

The Atlas API responds with "The service is currently unavailable" when a
key exceeds its request rate. Requests that fail this way are retried with
exponential backoff (shared across the concurrent workers) up to
\code{max.retries} times before the query fails. Lowering
\code{max.workers} reduces the request rate.
}
\examples{
\dontrun{
# requires an AlphaGenome API key in ALPHA_GENOME_API_KEY
avi <- LoadAVIFromAtlas(region = "chr11:5225000-5230000")
AVITrack(avi = avi, region = "chr11:5225000-5230000")
}
}
\references{
Cheng et al. (2026). AlphaGenome Atlas: in silico mutagenesis of
the entire human genome improves prioritization and interpretation of
non-coding variants
\url{https://deepmind.google.com/science/alphagenome/atlas}
}
\seealso{
\code{\link[=ListAtlasScorers]{ListAtlasScorers()}}, \code{\link[=ClearAtlasCache]{ClearAtlasCache()}}, \code{\link[=AVITrack]{AVITrack()}}
}
\concept{alphagenome}
