% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{AVITrack}
\alias{AVITrack}
\title{Plot AlphaGenome Atlas AVI SHAP feature attributions}
\usage{
AVITrack(
  avi,
  region,
  features = c("Expression", "Accessibility", "Total"),
  group.features = TRUE,
  type = "stacked",
  bins = 200,
  ymax = NULL,
  colors = NULL,
  y_label = NULL,
  show.axis = TRUE,
  api.key = NULL,
  max.workers = 4,
  max.retries = 5,
  verbose = FALSE
)
}
\arguments{
\item{avi}{AVI SHAP feature attributions. Either a \code{data.frame} with one
row per variant containing chromosome, position, ref and alt columns along
with one numeric column per feature (for example as returned by
\code{\link[=LoadAVIFromAtlas]{LoadAVIFromAtlas()}}), or \code{TRUE} to query the AlphaGenome Atlas API for the
region (see \code{\link[=LoadAVIFromAtlas]{LoadAVIFromAtlas()}}; requires the \strong{reticulate} package, the
\code{alphagenome} Python package, and an API key). If no variants fall in the
region an empty track is drawn.}

\item{region}{Genomic region (\link[GenomicRanges:GRanges]{GenomicRanges::GRanges} or a string that can
be converted to \code{GRanges} like "chr11:5225000-5230000")}

\item{features}{Features to plot, given as feature column names and/or
modality group labels (see Details), or "Total" for the total
AVI score. Groups without any feature in the data are skipped. Set to \code{NULL}
to plot all features. Features are stacked and listed in the legend in the
order given.}

\item{group.features}{Combine features into modality groups (see Details)
before plotting. If \code{FALSE}, each feature is
shown separately.}

\item{type}{Plot type. One of "stacked" (stacked bars showing the signed
contribution of each feature group) or "heatmap" (one row per feature
group, colored by SHAP value).}

\item{bins}{Number of bars (bins) to display across the region. Each bin
is \code{width(region) / bins} bp wide.}

\item{ymax}{Maximum absolute y-axis value for the "stacked" plot type. If
\code{NULL}, determined from the data.}

\item{colors}{Named vector of colors for feature groups (or features,
if \code{group.features = FALSE}). Groups without an entry use the default
palette.}

\item{y_label}{Y-axis label. If \code{NULL}, "AVI SHAP" is used, or "AVI score
(raw)" when only the total AVI score is plotted.}

\item{show.axis}{Show x-axis (default: TRUE)}

\item{api.key}{AlphaGenome API key, used when \code{avi = TRUE}. If
\code{NULL}, the \code{ALPHA_GENOME_API_KEY} environment variable is used.}

\item{max.workers}{Number of concurrent requests to the Atlas API when
\code{avi = TRUE} (see \code{\link[=LoadAVIFromAtlas]{LoadAVIFromAtlas()}}).}

\item{max.retries}{Maximum number of retries per throttled Atlas API
request when \code{avi = TRUE} (see \code{\link[=LoadAVIFromAtlas]{LoadAVIFromAtlas()}}).}

\item{verbose}{Show Atlas query progress messages when \code{avi = TRUE}.}
}
\value{
Returns a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object
}
\description{
Plot AlphaGenome Variant Impact (AVI) SHAP feature attributions from the
AlphaGenome Atlas across a genomic region. For each variant, the AlphaGenome
Atlas decomposes the raw AVI score into 18 additive SHAP contributions, one
per input feature (chromatin accessibility, TF binding, histone marks,
expression, splicing, protein impact, conservation, etc.). This function
summarizes those attributions along the genome and draws them as a track
that can be combined with other Signac genome browser tracks (see
\code{\link[=CoveragePlot]{CoveragePlot()}}).
}
\details{
The 18 AVI input features (named as in the AlphaGenome Atlas publication
and API) are grouped by modality for display:
\itemize{
\item \strong{Accessibility}: \code{MAX_ABS_ATAC}, \code{MAX_ABS_DNASE}
\item \strong{TF binding}: \code{MAX_ABS_CHIP_TF}
\item \strong{Histone}: \code{MAX_ABS_CHIP_HISTONE}
\item \strong{Expression}: \code{MAX_ABS_CAGE}, \code{MAX_ABS_PROCAP}, \code{MAX_ABS_RNA_SEQ}
\item \strong{Polyadenylation}: \code{MAX_ABS_POLYADENYLATION}
\item \strong{Splicing}: \code{MERGED_SPLICING}
\item \strong{Contact maps}: \code{MAX_ABS_CONTACT_MAPS}
\item \strong{Protein}: \code{ALPHAMISSENSE}, \code{PROTEIN_TERMINATION}, \code{START_LOST},
\code{STOP_LOST}
\item \strong{Conservation}: \code{PHASTCONS_470_WAY}, \code{CACTUS_241_WAY}
\item \strong{Indel}: \code{IS_INSERTION}, \code{IS_DELETION}
}

By default the Expression and Accessibility groups are shown. Other
features can be selected by column name or by group label, for example
\code{features = c("Accessibility", "Splicing")}, and \code{features = NULL}
shows all features. Only these 18 columns are treated as feature
attributions; other numeric columns, such as \code{AVI_SCORE}, are ignored
unless requested explicitly by name. The total impact score
(the raw AVI score, which equals the sum of all feature attributions) can
be requested with \code{features = "Total"}. In the "stacked" plot type the
total is drawn as points over the stacked feature contributions (as on the
AlphaGenome Atlas website) rather than being stacked itself. Note that the raw AVI score is on the model logit
scale; the PHRED-scaled AVI score shown on the AlphaGenome Atlas website can
be retrieved separately with \code{scorer = "AVI_SCORE"} in
\code{\link[=LoadAVIFromAtlas]{LoadAVIFromAtlas()}}.

The Atlas contains three SNVs at each genomic position (one per alternate
allele). Following the AlphaGenome Atlas publication, the variant with the
largest absolute total AVI score is kept at each position. The region is
then divided into \code{bins} equal-width bins and each bin shows the value of
largest magnitude among its positions, so that sharp SHAP peaks are
preserved while large regions remain fast to plot. Positions without a
variant contribute a value of zero.
}
\examples{
# simulated AVI SHAP feature attributions for three positions
avi <- data.frame(
  chromosome = "chr1",
  position = rep(c(713600, 713800, 714200), each = 3),
  ref = "A",
  alt = rep(c("C", "G", "T"), times = 3),
  MAX_ABS_ATAC = runif(9, 0, 1),
  MERGED_SPLICING = runif(9, 0, 2),
  CACTUS_241_WAY = runif(9, -0.5, 0.5)
)
# expression and accessibility features (default)
AVITrack(avi = avi, region = "chr1:713500-714500")
# all features, one row per modality group
AVITrack(avi = avi, region = "chr1:713500-714500", features = NULL,
         type = "heatmap")
# feature contributions with the total AVI score overlaid
AVITrack(avi = avi, region = "chr1:713500-714500",
         features = c("Accessibility", "Splicing", "Total"))

\dontrun{
# query the AlphaGenome Atlas API (requires ALPHA_GENOME_API_KEY)
AVITrack(avi = TRUE, region = "chr11:5225000-5230000")
}
}
\references{
Cheng et al. (2026). AlphaGenome Atlas: in silico mutagenesis of
the entire human genome improves prioritization and interpretation of
non-coding variants
\url{https://deepmind.google.com/science/alphagenome/atlas}
}
\concept{alphagenome}
\concept{visualization}
