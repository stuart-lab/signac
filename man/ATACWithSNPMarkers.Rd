% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{ATACWithSNPMarkers}
\alias{ATACWithSNPMarkers}
\title{Plot stacked ATAC coverage with SNP position markers}
\usage{
ATACWithSNPMarkers(
  object,
  region,
  snp.markers = NULL,
  group.by = NULL,
  assay = NULL,
  window = 100,
  track.colors = NULL,
  extend.upstream = 0,
  extend.downstream = 0,
  scales = "free_y",
  show.yaxis = TRUE
)
}
\arguments{
\item{object}{A Seurat object}

\item{region}{Genomic region to plot}

\item{snp.markers}{Data frame with columns: position (numeric), rsid (character),
color (character). Each row defines one SNP marker to display.}

\item{group.by}{Metadata column to create separate tracks (e.g., cell type or cluster)}

\item{assay}{Name of assay to use}

\item{window}{Smoothing window size}

\item{track.colors}{Optional named vector of colors for each track}

\item{extend.upstream}{Bases to extend region upstream}

\item{extend.downstream}{Bases to extend region downstream}

\item{scales}{Y-axis scales for facets: "free_y" (independent) or "fixed" (shared)}

\item{show.yaxis}{Show y-axis labels and ticks (default: TRUE). Set FALSE for cleaner
publication figures.}
}
\value{
Returns a ggplot2 object
}
\description{
Create publication-style figure showing ATAC coverage across multiple groups
as stacked horizontal tracks with specific SNPs highlighted in a separate track.
Designed to match publication figures showing chromatin accessibility across
cell types with variant positions marked.
}
\examples{
\dontrun{
# Define SNPs to mark
snps <- data.frame(
  position = c(112951996, 112952395),
  rsid = c("rs10885396", "rs7094871"),
  color = c("steelblue", "darkred")
)

# Create stacked plot
ATACWithSNPMarkers(
  object = atac_obj,
  region = "chr10-112990000-113010000",
  snp.markers = snps,
  group.by = "celltype",
  show.yaxis = FALSE
)
}
}
\concept{visualization}
